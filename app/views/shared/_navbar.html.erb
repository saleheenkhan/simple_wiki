<nav class="navbar navbar-toggleable-lg navbar-inverse fixed-top bg-primary">
  <%= image_tag("simple_wiki_logo@2x.png", class: 'navbar-brand NavigationBar-logo mr-6',  width: '156') %>
  <button class="navbar-toggler navbar-toggler-right" type="button" data-toggle="collapse" data-target="#navbarsDefault" aria-controls="navbarsDefault" aria-expanded="false" aria-label="Toggle navigation">
    <span class="navbar-toggler-icon"></span>
  </button>

    <div class="collapse navbar-collapse" id="navbarsDefault">
      <ul class="navbar-nav ml-sm-6 pl-sm-6">
      <%= form_with url: search_path, local: true, method: :get, html: { class: "form-inline my-2 my-lg-0" } do |form| %>
        <%= form.text_field :q, placeholder: "Search", data: { behavior: "autocomplete" }, class: "form-control mr-sm-2" %>
      <% end %>
      </ul>
      <ul class="navbar-nav ml-auto">
        <li class="nav-item dropdown">
          <a class="nav-link dropdown-toggle" data-toggle="dropdown" href="#" role="button" aria-haspopup="true" aria-expanded="false"><%= user_signed_in? ? current_user.full_name : "Guest User" %></a>
          <div class="dropdown-menu dropdown-menu-right">
            <%= link_to "New Article", new_article_path, class: 'dropdown-item' %>
            <div class="dropdown-divider"></div>
            <%= link_to "User Admin", user_admin_index_path, class: 'dropdown-item' if admin? %>
            <%= link_to "Account settings", edit_user_registration_path, class: "dropdown-item" if user_signed_in? %>
            <%= link_to "Logout", destroy_user_session_path, method: :delete, class: "dropdown-item" if user_signed_in? %>
            <%= link_to "Login", new_user_session_path, class: "dropdown-item" unless user_signed_in? %>
          </div>
        </li>
      </ul>
    </div>
</nav>
